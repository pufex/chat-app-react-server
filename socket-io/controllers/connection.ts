import type { EventController } from "../types";
import User from "../../db/auth/model";

export const handleDisconnect: EventController = (
    io,
    socket
) => {
    return async () => {
        
    }
}